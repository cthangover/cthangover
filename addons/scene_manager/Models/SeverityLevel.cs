#if TOOLS
namespace SceneManagerAddon
{
    /// <summary>
    /// Classification of a validation message's severity. Used by
    /// <see cref="ValidationMessage"/> and the validation panel to
    /// colour-code tree rows and to drive summary counts in the
    /// tab title.
    /// </summary>
    public enum SeverityLevel { Error, Warning, Info }
}
#endif
